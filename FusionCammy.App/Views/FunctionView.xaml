<UserControl x:Class="FusionCammy.App.Views.FunctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FusionCammy.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="680" d:DesignWidth="300">

    <Grid ClipToBounds="True" >
        <Rectangle Panel.ZIndex="0">
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF402090" Offset="0.0"/>
                    <GradientStop Color="#FF802090" Offset="0.5"/>
                    <GradientStop Color="#FF602090" Offset="1.0"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
            <Rectangle.Effect>
                <BlurEffect Radius="40"/>
            </Rectangle.Effect>
        </Rectangle>

        <Rectangle Fill="#66000000" Panel.ZIndex="1"/>

        <Border Background="Transparent" CornerRadius="8" Panel.ZIndex="2">
            <StackPanel Margin="20">

                <TextBlock Text="Fusion Cammy 😸" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="얼굴에 재미를 더해보세요" Foreground="#CCFFFFFF" Margin="0,5,0,20" TextWrapping="Wrap"/>

                <Border Height="1" Background="#33FFFFFF" Margin="0,10"/>

                <TextBlock Text="카메라 동작" Style="{StaticResource SectionHeaderStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- TODO : Command 할당 해야함 -->
                    <Button Grid.Column="0" Name="Button_StartCam" Content="📹 카메라 켜기" Style="{StaticResource FluentButtonStyle}" Margin="5"/>
                    <Button Grid.Column="1" Name="Button_StopCam" Content="📷 카메라 끄기" Style="{StaticResource FluentButtonStyle}" Margin="5"/>
                </Grid>
                <Button Name="Button_LoadImage" Content="📂 이미지 열기" Style="{StaticResource FluentButtonStyle}" Margin="5,0,5,5"/>

                <TextBlock Text="장식!" Style="{StaticResource SectionHeaderStyle}"/>
                <ItemsControl x:Name="DecoItemsList" Background="Transparent" BorderThickness="0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Style="{StaticResource DecoToggleButtonStyle}" Margin="5, 2"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Image Width="40" Height="40" Margin="0,0,15,0" Source="{Binding PreviewImagePath}" />
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontSize="15" FontWeight="Normal"/>
                                </Grid>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                    <!-- TODO : 애셋 바인딩 여기~~~~~~~~~~~~~~~~~~~ -->
                    <d:ItemsControl.ItemsSource />

                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>